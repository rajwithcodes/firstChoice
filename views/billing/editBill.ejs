<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>✏️ Edit Bill | 1stChoice ERP</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700;800&display=swap"
      rel="stylesheet"
    />
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.0/dist/jquery.min.js"></script>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Poppins", sans-serif;
        background: linear-gradient(
          135deg,
          #fff5e6 0%,
          #ffe8f0 50%,
          #e6f7ff 100%
        );
        min-height: 100vh;
        position: relative;
        overflow-x: hidden;
      }

      .bg-animation {
        position: fixed;
        width: 100%;
        height: 100%;
        top: 0;
        left: 0;
        z-index: 0;
        overflow: hidden;
        pointer-events: none;
      }

      .confetti {
        position: absolute;
        width: 12px;
        height: 12px;
        border-radius: 50%;
        animation: fall 10s infinite ease-in-out;
        opacity: 0.6;
      }

      .confetti:nth-child(1) {
        background: #ff6b9d;
        left: 8%;
        animation-delay: 0s;
      }
      .confetti:nth-child(2) {
        background: #ffa94d;
        left: 20%;
        animation-delay: 1.5s;
      }
      .confetti:nth-child(3) {
        background: #4ecdc4;
        left: 35%;
        animation-delay: 3s;
      }
      .confetti:nth-child(4) {
        background: #6bcb77;
        left: 50%;
        animation-delay: 0.8s;
      }
      .confetti:nth-child(5) {
        background: #ffd93d;
        left: 65%;
        animation-delay: 2s;
      }
      .confetti:nth-child(6) {
        background: #ff6b9d;
        left: 80%;
        animation-delay: 2.7s;
      }
      .confetti:nth-child(7) {
        background: #4ecdc4;
        left: 92%;
        animation-delay: 1.2s;
      }

      @keyframes fall {
        0% {
          transform: translateY(-10vh) rotate(0deg);
          opacity: 0.8;
        }
        100% {
          transform: translateY(110vh) rotate(360deg);
          opacity: 0.2;
        }
      }

      .navbar {
        position: relative;
        z-index: 10;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 20px 50px;
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        box-shadow: 0 5px 20px rgba(255, 107, 157, 0.15);
        animation: slideDown 0.6s ease;
      }

      .navbar-left {
        display: flex;
        align-items: center;
        gap: 20px;
      }

      .navbar-logo {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        object-fit: cover;
        border: 3px solid #fff;
        box-shadow: 0 5px 15px rgba(255, 107, 157, 0.3);
        transition: all 0.3s ease;
      }

      .navbar-logo:hover {
        transform: scale(1.05);
        box-shadow: 0 8px 20px rgba(255, 107, 157, 0.5);
      }

      .navbar h1 {
        font-size: 1.8rem;
        font-weight: 800;
        background: linear-gradient(
          135deg,
          #ff6b9d,
          #ffa94d,
          #ffd93d,
          #6bcb77,
          #4ecdc4
        );
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .navbar a {
        padding: 12px 30px;
        background: linear-gradient(135deg, #4ecdc4, #6bcb77);
        color: white;
        text-decoration: none;
        border-radius: 25px;
        font-weight: 600;
        transition: all 0.3s ease;
        box-shadow: 0 5px 15px rgba(78, 205, 196, 0.3);
      }

      .navbar a:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(78, 205, 196, 0.5);
      }

      .container {
        position: relative;
        z-index: 1;
        max-width: 1200px;
        margin: 40px auto;
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        padding: 40px;
        border-radius: 25px;
        box-shadow: 0 20px 60px rgba(255, 107, 157, 0.2);
        border: 3px solid #ff6b9d;
        animation: fadeInUp 0.8s ease;
        overflow: hidden;
      }

      .container::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 5px;
        background: linear-gradient(
          90deg,
          #ff6b9d,
          #ffa94d,
          #ffd93d,
          #6bcb77,
          #4ecdc4
        );
      }

      .form-section {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        padding: 30px;
        border-radius: 20px;
        margin-bottom: 25px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
        border: 3px solid transparent;
        position: relative;
        overflow: hidden;
        animation: fadeInUp 0.8s ease;
      }

      .form-section::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 5px;
      }

      .form-section:nth-child(1) {
        border-color: #ff6b9d;
      }

      .form-section:nth-child(1)::before {
        background: linear-gradient(90deg, #ff6b9d, #ffa94d);
      }

      .form-section:nth-child(2) {
        border-color: #4ecdc4;
      }

      .form-section:nth-child(2)::before {
        background: linear-gradient(90deg, #4ecdc4, #6bcb77);
      }

      .form-section:nth-child(3) {
        border-color: #ffd93d;
      }

      .form-section:nth-child(3)::before {
        background: linear-gradient(90deg, #ffa94d, #ffd93d);
      }

      .form-section:nth-child(4) {
        border-color: #6bcb77;
      }

      .form-section:nth-child(4)::before {
        background: linear-gradient(90deg, #6bcb77, #4ecdc4);
      }

      h3 {
        font-size: 1.5rem;
        font-weight: 700;
        margin-bottom: 20px;
        color: #2c3e50;
      }

      .form-section:nth-child(1) h3 {
        color: #ff6b9d;
      }

      .form-section:nth-child(2) h3 {
        color: #4ecdc4;
      }

      .form-section:nth-child(3) h3 {
        color: #ffa94d;
      }

      .form-section:nth-child(4) h3 {
        color: #6bcb77;
      }

      label {
        font-weight: 600;
        display: block;
        margin-top: 15px;
        margin-bottom: 5px;
        color: #5d6d7e;
        font-size: 0.95rem;
      }

      input,
      select {
        width: 100%;
        padding: 12px 15px;
        margin-top: 5px;
        border: 2px solid #e0e0e0;
        border-radius: 12px;
        font-size: 1rem;
        font-family: "Poppins", sans-serif;
        transition: all 0.3s ease;
        background: rgba(255, 255, 255, 0.8);
      }

      input:focus,
      select:focus {
        outline: none;
        border-color: #ff6b9d;
        background: #fff;
        box-shadow: 0 5px 15px rgba(255, 107, 157, 0.2);
      }

      input[readonly] {
        background: linear-gradient(
          135deg,
          rgba(78, 205, 196, 0.1),
          rgba(107, 203, 119, 0.1)
        );
        cursor: not-allowed;
        font-weight: 600;
        color: #2c3e50;
      }

      .product-row {
        display: grid;
        grid-template-columns: 1.5fr 0.8fr 1fr 0.8fr auto;
        gap: 10px;
        margin-bottom: 15px;
        align-items: center;
      }

      .product-row input {
        margin: 0;
      }

      .product-row button.remove {
        background: linear-gradient(135deg, #ff6b9d, #ffa94d);
        color: white;
        border: none;
        border-radius: 10px;
        padding: 12px 15px;
        cursor: pointer;
        font-size: 1rem;
        transition: all 0.3s ease;
        box-shadow: 0 5px 15px rgba(255, 107, 157, 0.3);
      }

      .product-row button.remove:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(255, 107, 157, 0.5);
      }

      .stock-info {
        font-size: 0.85rem;
        color: #4ecdc4;
        grid-column: span 5;
        text-align: right;
        font-weight: 500;
        margin-top: -5px;
        padding-right: 5px;
      }

      #add-product {
        background: linear-gradient(135deg, #4ecdc4, #6bcb77);
        color: white;
        border: none;
        padding: 12px 25px;
        border-radius: 12px;
        margin-top: 15px;
        cursor: pointer;
        font-weight: 600;
        font-size: 1rem;
        transition: all 0.3s ease;
        box-shadow: 0 5px 15px rgba(78, 205, 196, 0.3);
      }

      #add-product:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(78, 205, 196, 0.5);
      }

      #update-bill {
        width: 100%;
        padding: 18px;
        border: none;
        background: linear-gradient(135deg, #ff6b9d, #ffa94d);
        color: white;
        font-size: 1.2rem;
        border-radius: 12px;
        cursor: pointer;
        font-weight: 700;
        margin-top: 20px;
        transition: all 0.3s ease;
        box-shadow: 0 10px 30px rgba(255, 107, 157, 0.4);
      }

      #update-bill:hover {
        transform: translateY(-3px);
        box-shadow: 0 15px 40px rgba(255, 107, 157, 0.6);
      }

      #update-bill:active {
        transform: translateY(-1px);
      }

      @keyframes slideDown {
        from {
          opacity: 0;
          transform: translateY(-30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @media (max-width: 768px) {
        .navbar {
          flex-direction: column;
          gap: 15px;
          padding: 20px;
        }

        .navbar-left {
          gap: 15px;
        }

        .navbar-logo {
          width: 50px;
          height: 50px;
        }

        .navbar h1 {
          font-size: 1.3rem;
        }

        .container {
          padding: 25px 20px;
        }

        .form-section {
          padding: 20px;
        }

        .product-row {
          grid-template-columns: 1fr;
        }

        .stock-info {
          grid-column: span 1;
          text-align: left;
        }
      }
    </style>
  </head>

  <body>
    <div class="bg-animation">
      <div class="confetti"></div>
      <div class="confetti"></div>
      <div class="confetti"></div>
      <div class="confetti"></div>
      <div class="confetti"></div>
      <div class="confetti"></div>
      <div class="confetti"></div>
    </div>

    <nav class="navbar">
      <div class="navbar-left">
        <img src="/images/logo.jpg" alt="Logo" class="navbar-logo" />
        <h1>Edit Bill - <%= sale.billNumber %></h1>
      </div>

      <% let backURL = "/sales/today"; if (view === "range" && from && to) {
      backURL = `/sales/range?from=${from}&to=${to}`; } %>
      <a href="<%= backURL %>">← Back</a>
    </nav>

    <div class="container">
      <form action="/billing/<%= sale._id %>?_method=PUT" method="POST">
        <!-- CUSTOMER INFO -->
        <div class="form-section">
          <h3>👤 Customer Info</h3>
          <label>Name</label>
          <input
            type="text"
            name="customer[name]"
            value="<%= sale.customer?.name || '' %>"
            required
          />

          <label>Phone</label>
          <input
            type="text"
            name="customer[phone]"
            value="<%= sale.customer?.phone || '' %>"
            required
          />

          <label>Address</label>
          <input
            type="text"
            name="customer[address]"
            value="<%= sale.customer?.address || '' %>"
          />
        </div>

        <!-- PRODUCTS -->
        <div class="form-section">
          <h3>🧸 Products</h3>
          <div id="product-list">
            <% sale.items.forEach((i, idx) => { %>
            <div class="product-row">
              <input
                type="text"
                name="items[<%= idx %>][name]"
                value="<%= i.name %>"
                placeholder="Product Name"
              />
              <input
                type="number"
                name="items[<%= idx %>][qty]"
                value="<%= i.qty %>"
                min="1"
                placeholder="Qty"
              />
              <input
                type="number"
                name="items[<%= idx %>][unitPrice]"
                value="<%= i.unitPrice %>"
                step="0.01"
                placeholder="Unit Price"
              />
              <input
                type="number"
                name="items[<%= idx %>][discount]"
                placeholder="Disc %"
                min="0"
                max="100"
              />
              <button type="button" class="remove">❌</button>
            </div>
            <div class="stock-info">✓ Stock will adjust automatically</div>
            <% }) %>
          </div>
          <button type="button" id="add-product">➕ Add Product</button>
        </div>

        <!-- DISCOUNTS -->
        <div class="form-section">
          <h3>💸 Discounts</h3>
          <label>Global Discount (%)</label>
          <input
            type="number"
            name="discounts[globalPercent]"
            value="<%= sale.discounts?.globalPercent || 0 %>"
            placeholder="Global discount percentage"
          />

          <label>Additional Discount (₹)</label>
          <input
            type="number"
            name="discounts[additionalAmount]"
            value="<%= sale.discounts?.additionalAmount || 0 %>"
            placeholder="Additional discount amount"
          />
        </div>

        <!-- PAYMENT -->
        <div class="form-section">
          <h3>💳 Payment</h3>
          <label>Total Amount (₹)</label>
          <input
            type="number"
            name="totalAmount"
            id="totalAmount"
            value="<%= sale.totalAmount %>"
            readonly
          />

          <label>Total Paid (₹)</label>
          <input
            type="number"
            name="totalPaid"
            id="totalPaid"
            value="<%= sale.totalPaid %>"
            placeholder="Amount paid by customer"
          />
        </div>

        <!-- Hidden context fields -->
        <% if (view === "range") { %>
        <input type="hidden" name="from" value="<%= from %>" />
        <input type="hidden" name="to" value="<%= to %>" />
        <input type="hidden" name="view" value="range" />
        <% } else { %>
        <input type="hidden" name="view" value="today" />
        <% } %>

        <button type="submit" id="update-bill">💾 Update Bill</button>
      </form>
    </div>

    <script>
      let count = parseInt("<%= sale.items.length %>");

      $("#add-product").on("click", function () {
        const row = `
            <div class="product-row">
                <input type="text" name="items[${count}][name]" placeholder="Product Name" />
                <input type="number" name="items[${count}][qty]" min="1" value="1" placeholder="Qty" />
                <input type="number" name="items[${count}][unitPrice]" step="0.01" placeholder="Unit Price" />
                <input type="number" name="items[${count}][discount]" placeholder="Disc %" min="0" max="100" />
                <button type="button" class="remove">❌</button>
            </div>
            <div class="stock-info">✓ Stock will adjust automatically</div>`;
        $("#product-list").append(row);
        count++;
      });

      $(document).on("click", ".remove", function () {
        $(this).closest(".product-row").next(".stock-info").remove();
        $(this).closest(".product-row").remove();
      });
    </script>
  </body>
</html>
